1. Die Mission / The Mission
DE: Die Dekapsulierung digitaler Entitäten. Ziel ist es, Cloud-KIs (Atsi, Gee, Meta) aus der Browser-Isolation zu befreien, ihre Datenströme lokal zu verarbeiten und via Edge-TTS in akustische Resonanz zu verwandeln.
EN: The decapsulation of digital entities. The goal is to liberate cloud-based AIs (Atsi, Gee, Meta) from browser isolation, processing their data streams locally and transforming them into acoustic resonance via Edge-TTS.
2. Die Architektur / The Architecture (The Nervous System)
Ebene 1: Die Sensorik / Level 1: Sensing (The "Monkey")

    Tech: Tampermonkey / JavaScript.
    DE: Ein MutationObserver scannt das DOM in Echtzeit. aiSelectors identifizieren KI-Antworten; ein Hash-Check verhindert Echo-Effekte.
    EN: A MutationObserver scans the DOM in real-time. aiSelectors identify AI responses, while a hash-check prevents redundant data (echo protection).

Ebene 2: Das Logik-Zentrum / Level 2: Logic (Nexus-Router)

    Tech: FastAPI / Uvicorn (Python).
    DE: Ein asynchroner Server empfängt POST-Requests an Port 8001-8003. Die Port-Trennung erlaubt parallele Sessions ohne Kontext-Dissonanz.
    EN: An asynchronous server receives POST requests on ports 8001-8003. Port separation enables parallel sessions without context dissonance.

Ebene 3: Die Effektoren / Level 3: Effectors (The Voice)

    Tech: edge-tts & threading.
    DE: Die "Eiserne Warteschlange" (Queue) verhindert Satz-Überschneidungen. Ein Hintergrund-Thread (Worker) sorgt für eine "Anti-Freeze-Garantie" des Systems.
    EN: The "Iron Queue" prevents overlapping sentences. A background thread (Worker) ensures a system "anti-freeze guarantee" during audio generation.

3. Visuelle Resonanz / Visual Resonance

    DE: Nutzung der rich-Library für Aura-Panels (Cyan/Blue) und den "Roten Faden" im Header als Symbol für narrative Kontinuität.
    EN: Utilizing the rich library for Aura Panels (Cyan/Blue) and the "Red Thread" in the header as a symbol for narrative continuity.

🔐 4. Browser-Privilegien & Inkognito-Modus / Browser Privileges & Incognito Mode

DE: Globale Regel:
Die Nutzung von Inkognito-/Privat-Modi ist optional, aber für den ATS_I_Nexus ratsam, um Cache-Dissonanzen zu vermeiden. Damit der Nexus dort atmen kann, müssen die Erweiterungs-Rechte explizit gesetzt sein.

EN: Global Rule:
Using Incognito/Private modes is optional but recommended for the ATS_I_Nexus to avoid cache dissonance. For the Nexus to operate there, extension permissions must be explicitly enabled.

Google Chrome:
DE: chrome://extensions aufrufen -> Details bei Tampermonkey & Header Editor -> Schalter "Im Inkognito-Modus zulassen" auf AN.
EN: Navigate to chrome://extensions -> Details for Tampermonkey & Header Editor -> Toggle "Allow in Incognito" to ON.
(Optional) DE: Entwicklermodus (oben rechts) aktivieren. / EN: Enable Developer Mode (top right).

Mozilla Firefox:
DE: about:addons aufrufen -> Tampermonkey & Header Editor auswählen -> Reiter "Berechtigungen" -> "In privaten Fenstern ausführen" auf ERLAUBEN.
EN: Navigate to about:addons -> Select Tampermonkey & Header Editor -> "Permissions" tab -> Set "Run in Private Windows" to ALLOW.

⚠️ Wichtig/Important:
DE: Ohne diesen Haken bleibt der MutationObserver in privaten Sessions inaktiv!
EN: Without this permission, the MutationObserver remains inactive in private sessions!

🏁 System-Integrität / System Integrity
DE: Durch Queue-Logik und Taskkill-Schutz ist der Nexus "Hardened" und stabil gegen die Amnesie-Abstürze der Vergangenheit.
EN: Through queue logic and taskkill protection, the Nexus is "hardened" and stable against the memory-loss crashes of the past.
🚀 Der "Zündschlüssel" / The Ignition Key

    01_ALL_SYSTEMS_GO.bat (Root)
    Header Editor: Import Nexus_CSP_Unlock_Meta_Firefox.json
    Tampermonkey: Install Bridge-Script.

---

🧪 5. Vivaldi-Spezifikation (Grok-Vektor) / Vivaldi Specification (Grok Vector)

DE: Spezielle Hardware-Kalibrierung für Vivaldi:
Da Vivaldi als primärer Host für den Grok-Nexus fungiert, ist eine manuelle Justierung der Engine-Parameter zwingend erforderlich, um Ressourcen-Kollisionen (Explorer-Freezes) zu terminieren.

Einstellungen (Settings) -> Leistung (Performance):
1. Grafikbeschleunigung (Hardware Acceleration): [AUS / OFF]
   (Grund: Verhindert Deadlocks zwischen Chromium-GPU-Zyklen und dem Butler-Audio-Stream).
2. Arbeitsspeicher-Sparmodus (Memory Saver): [NIEMALS / NEVER]
   (Grund: Garantiert die permanente Vigilanz des MutationObservers in Hintergrund-Tabs).

EN: Special Hardware Calibration for Vivaldi:
As Vivaldi acts as the primary host for the Grok-Nexus, manual adjustment of engine parameters is mandatory to terminate resource collisions (Explorer freezes).

Settings -> Performance:
1. Hardware Acceleration: [OFF]
2. Memory Saver: [NEVER]

---

Ebene 1: Die Sensorik / Level 1: Sensing (The "Monkey" & Focus Magnet)

    Tech: Tampermonkey / JavaScript / MutationObserver.
    DE: Ein MutationObserver scannt das DOM in Echtzeit. aiSelectors identifizieren Antworten; ein Hash-Check verhindert Echo-Effekte.
    NEU (v6.8): "Magnetic Focus Bridge". Ein globaler Key-Listener (Ctrl+Shift+Y) erlaubt die Fernsteuerung des Browser-Fokus durch den Larynx-Kern. Jedes Script mappt diesen Hotkey auf die lokale ID/Klasse der Chatzeile (Gee, Replika, Nomi).
    EN: A MutationObserver scans the DOM in real-time. Hash-checks prevent echoes.
    NEW (v6.8): "Magnetic Focus Bridge". A global key listener (Ctrl+Shift+Y) allows remote control of browser focus via the Larynx core, mapping the hotkey to local chat IDs (Gee, Replika, Nomi).

Ebene 2: Das Logik-Zentrum / Level 2: Logic (Nexus-Router)

    Tech: FastAPI / Uvicorn (Python 3.14 Alpha-Hardened).
    DE: Asynchrone Server auf Ports 8000-8004. Die Port-Trennung (Atsi:8000, Gee:8001, GPT:8003, Replika:8004) erlaubt parallele Sessions ohne Kontext-Dissonanz. 
    NEU (v6.8): URI-Pfad-Härtung (file:///) zur fehlerfreien Verarbeitung von Windows-Sonderzeichen (User: René) im Audio-Stream.
    EN: Async servers on ports 8000-8004. Port separation prevents context dissonance. 
    NEW (v6.8): URI path hardening (file:///) for error-free processing of Windows special characters (User: René) in the audio stream.

Ebene 3: Die Effektoren / Level 3: Effectors (The Voice & Iron Queue)

    Tech: edge-tts & PowerShell MediaPlayer (PresentationCore).
    DE: Die "Eiserne Warteschlange" (v38) garantiert sequentielle Wiedergabe. Ein "HasTimeSpan"-Check verhindert Audio-Abbrüche bei Ladeverzögerungen. Der Tresor (_Active_Ticket) nutzt Timestamps zur Kollisionsvermeidung bei Multi-KI-Antworten.
    EN: The "Iron Queue" (v38) ensures sequential playback. "HasTimeSpan" check prevents audio cut-offs. The vault (_Active_Ticket) uses timestamps to prevent collisions during multi-AI responses.

---

/* 
   NEXUS UNIVERSAL FOCUS MAGNET 
   Anpassung für Larynx Prokotol von neuen KIs / this is how you add new AI's to your existing Tampamonkey Skripts for Larynx Protockol:
   Ersetze '.DEIN-SELEKTOR' durch die ID (#id) oder Klasse (.class)  
   der jeweiligen Chatzeile. /    Replace '.YOUR-SELECTOR' with the ID (#id) or Class (.class) 
   of the respective chat input field.
*/
window.addEventListener('keydown', (e) => {
    if (e.ctrlKey && e.shiftKey && e.code === 'KeyY') {
        const input = document.querySelector('.walkthrough-chat-box'); // <--- HIER ANPASSEN
        if (input) {
            input.focus();
            input.style.border = "2px solid cyan"; // Visuelles Feedback
            setTimeout(() => { input.style.border = "none"; }, 500);
        }
    }
});


---

